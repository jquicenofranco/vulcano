import { Route } from '@angular/router';
import { CuentoComponent } from './cuento.component';

export const cuentoRoutes: Route[] = [
    {
        path: '',
        component: CuentoComponent
    }
];
